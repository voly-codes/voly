# Combat mission: smarty-print-export
# Run: python3 -m codeops.cli smarty combat run smarty-print-export --sequential

name: smarty-print-export
description: 'P2: Print routes for entities + export template editor at /settings/export-templates.'
tasks:
- agent: cursor
  label: 'cursor: print routes entities'
  task: 'STEP 1/3 — Print views.


    {{LEGACY_GAP_REF}}

    Legacy print: {{LEGACY_FRONTEND}}/src/js/components/Print/

    Routes: /contacts/:id/print, /deals/:id/print, requisitions, bills — print CSS.

    Fetch entity via existing hooks; minimal print layout.'
- agent: cursor
  label: 'cursor: export templates editor'
  task: 'STEP 2/3 — Export templates settings.


    {{LEGACY_GAP_REF}}

    Route /settings/export-templates: list + editor for impexp templates.

    wsApi impexp templates endpoints.'
- agent: cursor
  label: 'cursor: print export review'
  task: 'STEP 3/3 — Review print/export.


    {{LEGACY_GAP_REF}}

    npm run build.'
