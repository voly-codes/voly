# Combat mission: smarty-time-log
# Run: python3 -m voly.cli smarty combat run smarty-time-log --sequential

name: smarty-time-log
description: 'P1: Time log — workspace settings + employee embed + PDF export + card
  block.'
tasks:
- agent: cursor
  label: 'cursor: time log API settings page'
  task: 'STEP 1/3 — Time log settings.


    {{LEGACY_GAP_REF}}

    Legacy: {{LEGACY_FRONTEND}}/src/js/components/TimeLog/

    Route /settings/time-log; wsApi time_log endpoints; hooks useTimeLog.

    Workspace defaults: rounding, billable flags.'
- agent: cursor
  label: 'cursor: time log card embed export'
  task: 'STEP 2/3 — Card embed + PDF export.


    {{LEGACY_GAP_REF}}

    TimeLogBlock on deal/assignment detail tabs (requires card-linked-tabs).

    Employee time-log view; PDF export route or print stylesheet.'
- agent: cursor
  label: 'cursor: time log review'
  task: 'STEP 3/3 — Review time log.


    {{LEGACY_GAP_REF}}

    npm run build.'
