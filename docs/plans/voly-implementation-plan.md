# VOLY — План внедрения (чеклист)

**Дата:** 2026-07-04
**Источник истины:** [voly-roadmap-and-monetization.md](voly-roadmap-and-monetization.md) — этот чеклист лишь операционализирует его этапы в отслеживаемые задачи. При расхождении приоритетов правит roadmap.
**Сопутствующие:** [codeops-project-assessment.md](codeops-project-assessment.md) (риски R1–R7), [omniroute-analysis-for-voly.md](omniroute-analysis-for-voly.md) (что переносим из OmniRoute).

## Легенда статусов

- `[x]` — сделано (есть коммит/файл)
- `[ ]` — не начато
- `[~]` — частично (пояснение в скобках)

Порядок работ из roadmap §7: сначала надёжное бесплатное ядро (этапы 0–3), затем платный мультитенантный слой (этапы 4–5); тестовое покрытие (этап 6) — сквозное.

---

## Этап 0 — Гигиена и честность (S)

- [x] Синхронизировать README/доки: дефолт `a2a.enabled`, порты, статусы планов — *коммит c1938e5; порты выправлены на 7788 (web ui) / 9202 (pipeline serve), `a2a.enabled=true`, устаревший план a2a-auto-dispatch удалён*
- [x] Переименование проекта codeops → voly (пакет, env-переменные, доки) — *коммиты 5c58d40, a3fd2ef; `CODEOPS_*` → `VOLY_*`*
- [ ] CI-гейт `check:env-doc-sync` (каждая env-переменная из кода обязана быть в `.env.example` и в доке)
- [ ] CI-гейт `check:docs-sync` / `check:doc-links`
- [ ] Пометить/вынести прототипы (workflow engine, project scanner) — статус в §11 п.3 roadmap открыт (заморозить/вырезать/отдельный трек)
- [x] Сжать логотип — *SVG-источник + PNG-экспорт `docs/assets/voly-logo.png` (~64 КБ, 2400px)*

**Готовность:** CI падает при рассинхроне доков; репозиторий без фактических противоречий.
**Осталось для готовности:** CI-гейты доков (2 пункта), решение по прототипам.

---

## Этап 1 — Стабилизация executor path (M) · *фундамент FinOps*

- [x] `isEmptyContentResponse` guard — *подключён на уровне gateway: провайдер-адаптеры (`providers.py`) пробрасывают `stop_reason`; `AIGateway._empty_content_error` конвертирует фейк-успех в model-fallback в `_gateway_call`, `chat()`-direct и `_direct_fallback`; легитимный пустой (`max_tokens`/`tool_use`/`length`/`tool_calls`) не триггерит fallback. Тесты: `tests/test_ai_gateway.py`, `tests/test_error_classifier.py`; doc: `docs/backend/ai-gateway.md`*
- [x] Пересмотр ключа кэша executor path + документирование границ валидности (R1) — *`AIGateway._cache_key` включает project-state scope (`project_state.py:project_fingerprint` — git HEAD + dirty-сигнатура, repo-level; opt-in `files=` для file-level); `gw.cache_scope` из `config.default_cwd` в `pipeline/core.py`. Аудит: executor-субпроцессы и wrangler `/infer` вне кэша — кэшируется только text-only reasoning. Границы задокументированы в `docs/backend/ai-gateway.md`. Тесты: `tests/test_project_state.py`, `tests/test_ai_gateway.py`*
- [ ] Таймауты на subprocess-executor-ах (все CLI-обёртки)
- [ ] Retry-aware стоимость в TaskEvent и `_COST_RATES` (перезапуски не задваивают цифры)

**Готовность:** tool-use не вызывает ложных fallback; кэш инвалидируется при изменении репозитория; стоимость корректна при перезапусках.
**Монетизация:** телеметрия стоимости становится достоверной — базис для FinOps-отчётов.

---

## Этап 2 — Устойчивость цепочки (M) · *аргумент платного tier*

- [ ] Heartbeat + watchdog поверх TaskEvent
- [ ] Локальный чекпоинтинг между шагами мульти-агента (в `.voly/`, согласуется с «runtime state is not source»)
- [ ] Git-чекпоинт перед ретраем file-capable executor-а (идемпотентность)

**Открытый вопрос (roadmap §6):** реальная длина цепочек и частота зависаний — при коротких цепочках достаточно таймаутов + heartbeat (ступени 1–2). **Не внедрять Temporal**; при доказанной необходимости — DBOS/Restate.

**Готовность:** краш посреди цепочки не теряет работу; ретрай не задваивает файловые изменения.

---

## Этап 3 — Границы и делегирование gateway (M)

- [ ] Формализовать A/B-слои и make-vs-delegate в `docs/ARCHITECTURE.md`
- [ ] Делегирование провайдерной маршрутизации внешнему gateway первым классом; прямые адаптеры — fallback
- [x] Семантическая классификация billing-ошибок на уровне CLI-executor-ов — *`voly/ai_gateway/error_classifier.py` + делегация `_is_billing_error`, коммит 769bc89; rate-limit (429) отделён от quota-exhausted, 21 тест*

**Готовность:** документировано и реализовано, что VOLY маршрутизирует агентов, а инференс делегирует.

---

## Этап 4 — Мультитенантный фундамент (L) · *ядро платного продукта (Team tier)*

- [ ] Хранилище задач/событий с мультитенантностью
- [ ] Аутентификация, роли, доступы, SSO
- [ ] Организационные spend limits и cost policy
- [ ] Аудит-лог всех запусков
- [ ] Shared task history (история и поиск задач между разработчиками)

**Готовность:** несколько разработчиков одной организации видят общий дашборд расходов, админ ставит лимиты на команду.
**Монетизация:** это и есть Team tier — первый платный продукт.
**Риск (roadmap §11):** изоляция данных организаций критична — ошибка здесь дороже любого технического бага.

---

## Этап 5 — Managed federation (M) · *инфраструктурный платный сервис*

- [ ] Хостинг A2A-хаба и агент-воркеров как сервис
- [ ] Биллинг по использованию / seat
- [ ] Онбординг без поднятия CF-стека пользователем

**Готовность:** команда получает распределённую оркестрацию без собственной Cloudflare-инфры.

---

## Этап 6 — Тестовое покрытие критических путей (L, СКВОЗНОЙ)

> Идёт параллельно, не после. Без него платный tier нельзя продавать.

- [ ] Интеграционный тест: полный проход billing fallback до zen
- [ ] Интеграционный тест: spend limit останавливает мульти-агентную цепочку посередине
- [ ] Интеграционный тест: все три условия recursion guard — *частично покрыто `tests/test_a2a_p0.py` (`VOLY_A2A_NESTED`)*
- [ ] Контрактные тесты CLI-обёрток (claude/cursor/opencode) + метрика доли нераспознанных ошибок
- [ ] Тесты устойчивости цепочки (этап 2) и семантики кэша (этап 1)
- [ ] Мультитенантные тесты изоляции (данные организаций не текут между собой) — блокер для этапа 4
- [x] Юнит-тесты классификатора ошибок — *`tests/test_error_classifier.py`, 21 тест*

---

## Этап 7 — Периферия и marketplace (по необходимости)

- [ ] Circuit breaker + cooldown-память — *только при доказанной проблеме (OmniRoute-порт P2)*
- [ ] `skipForToolRequests` перед last-resort в billing chain — *zen может не тянуть tool-use*
- [ ] Marketplace скилов с revenue share — *только при наличии базы пользователей*
- [ ] Публикация воспроизводимого бенчмарка экономии — *сильнейший маркетинговый актив; телеметрия уже есть*
- [ ] Заменить `local context` (grep) на knowledge-graph MCP перед executor — *внешний инструмент `codebase-memory-mcp` через `voly mcp`, не форк; см. `codebase-memory-mcp-analysis-for-voly.md`. Только после стабилизации ядра B*

---

## Явные не-цели (roadmap §10)

- [ ] ~~Конкурировать с OmniRoute/LiteLLM по числу провайдеров~~ — слой A делегируется
- [ ] ~~Внедрять Temporal~~ — при необходимости DBOS/Restate
- [ ] ~~Писать свой workflow-движок~~ — durable execution из внешней библиотеки
- [ ] ~~Сливаться с проектами под ограничительными лицензиями~~ — только интеграция (урок Multica)
- [ ] ~~Закрывать ядро~~ — платится за хостинг и командную обвязку
- [ ] ~~Запускать marketplace рано~~
- [ ] ~~Наращивать периферию~~ до стабилизации ядра B и покрытия тестами

---

## Открытые вопросы, блокирующие планирование (roadmap §11)

1. [ ] Реальная длина мульти-агентных цепочек и частота зависаний → глубина этапа 2
2. [ ] Насколько глубоко делегировать слой A — полностью или гибрид → этап 3
3. [ ] Судьба workflow-прототипа и project-scanner — заморозить/вырезать/отдельный трек → этап 0
4. [ ] Приоритет платных этапов: Team tier (этап 4) или managed federation (этап 5) первым
5. [ ] Self-hosted enterprise vs чистый SaaS для Team tier → архитектура мультитенантности
6. [ ] Партнёрство с task-платформами (Multica и аналоги) как канал — инвестировать ли в interop рано

---

## Сводка прогресса

| Этап | Готовность | Примечание |
|---|---|---|
| 0 — Гигиена | 3 / 6 | осталось: 2 CI-гейта доков + решение по прототипам |
| 1 — Executor path | 2 / 4 | guard + cache-key scope (R1); следующий — таймауты subprocess |
| 2 — Устойчивость | 0 / 3 | ждёт ответа на открытый вопрос №1 |
| 3 — Границы/делегирование | 1 / 3 | классификатор billing готов |
| 4 — Мультитенантность | 0 / 5 | первый платный этап |
| 5 — Managed federation | 0 / 3 | |
| 6 — Тесты (сквозной) | 1.5 / 7 | классификатор + частично recursion guard |
| 7 — Периферия | 0 / 4 | по необходимости |

**Ближайший фокус (roadmap §7, ядро прежде монетизации):** доделать этап 1 — таймауты subprocess-executor-ов и retry-aware стоимость в `TaskEvent`/`_COST_RATES` (guard R1-cache-key закрыты).
